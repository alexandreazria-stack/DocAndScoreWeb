/** Polling interval for localStorage session refresh */
export const POLL_INTERVAL_MS = 3000;

/** Delay before navigating to result after session completes */
export const SESSION_RESULT_DELAY_MS = 1500;

/** Duration the "Copié !" feedback stays visible */
export const COPIED_RESET_MS = 2000;

/** Delay before patient redirect after answering last question */
export const PATIENT_REDIRECT_DELAY_MS = 350;
